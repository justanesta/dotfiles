# ~/.profile: executed by the command interpreter for login shells.
# Managed by chezmoi - machine: {{ .machine }}

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/.local/bin" ] ; then
    PATH="$HOME/.local/bin:$PATH"
fi

# Custom scripts
if [ -d "$HOME/code/claude-code-resources/bin" ]; then
    PATH="$HOME/code/claude-code-resources/bin:$PATH"
fi

# pyenv: make shims available early on PATH for login shells
# export PYENV_ROOT="$HOME/.pyenv"
# if [ -d "$PYENV_ROOT/bin" ]; then
#     PATH="$PYENV_ROOT/bin:$PATH"
# fi

# if command -v pyenv >/dev/null 2>&1; then
#     eval "$(pyenv init --path)"
# fi

{{- if eq .chezmoi.os "darwin" }}

# Homebrew setup for login shells
if [ -f /opt/homebrew/bin/brew ]; then
    eval "$(/opt/homebrew/bin/brew shellenv)"
fi
{{- end }}

export PATH